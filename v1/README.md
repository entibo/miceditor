# Miceditor

Map editor for Transformice

# Contribute

* Clone the repository
* Install the dependencies: `npm i`
* Run the development server (localhost:5000): `npm run dev`
* Build: `npm run build`

Translations are handled externally, contact me directly to get access
